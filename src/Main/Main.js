import React, { Component } from 'react';
import HomePage from '../HomePage/HomePage';

class Main extends Component {
    render() {
        return (
            <main>
                <HomePage />
            </main>
        );
    }
}

export default Main;